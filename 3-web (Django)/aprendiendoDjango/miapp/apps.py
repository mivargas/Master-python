from django.apps import AppConfig


class MiappConfig(AppConfig):
    name = 'miapp'
    verbose_name ='Mi primera aplicacion' #el nombre que quiero que se visualice en el panel de administracion (nota se debe hacer referencia a MiappConfig en la app registrada en settings.py)
